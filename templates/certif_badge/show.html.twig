{% extends 'base.html.twig' %}

{% block title %}CertifBadge{% endblock %}

{% block body %}
    <h1>CertifBadge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certif_badge.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_certif_badge_index') }}">back to list</a>

    <a href="{{ path('app_certif_badge_edit', {'id': certif_badge.id}) }}">edit</a>

    {{ include('certif_badge/_delete_form.html.twig') }}
{% endblock %}
