<form method="post" action="{{ path('app_evennement_delete', {'id': evennement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evennement.id) }}">
    <input type="submit" value="Delete" class="btn btn-primary">
</form>
