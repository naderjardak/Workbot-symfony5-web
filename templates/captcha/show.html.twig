{% extends 'base.html.twig' %}

{% block title %}Captcha{% endblock %}

{% block body %}
    <h1>Captcha</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ captcha.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ captcha.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_captcha_index') }}">back to list</a>

    <a href="{{ path('app_captcha_edit', {'id': captcha.id}) }}">edit</a>

    {{ include('captcha/_delete_form.html.twig') }}
{% endblock %}
