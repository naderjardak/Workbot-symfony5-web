<form method="post" action="{{ path('app_captcha_delete', {'id': captcha.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ captcha.id) }}">
    <button class="btn">Delete</button>
</form>
