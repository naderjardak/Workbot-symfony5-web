{% extends 'utilisateur/client/base.html.twig' %}

{% block body %}



    <h1>Badge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ badge.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ badge.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_badge_index') }}">back to list</a>
{% endblock %}
